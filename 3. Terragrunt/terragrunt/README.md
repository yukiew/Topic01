```bash
  # 等於 terraform init, 不過多了一步,會下載 terraform 程式碼
  $ terragrunt init
  
  $ cd dev 
  # 等於 terrform plan
  $ terragrunt plan 
  
  # 等於 terraform apply
  $ terragrunt apply 
  
  # 等於 terraform destroy
  $ terragrunt destroy
```

